/// <reference types="node" />
import { RadixSerializable } from '../RadixAtomModel';
import BN from 'bn.js';
export default class RadixEUID implements RadixSerializable {
    static SERIALIZER: string;
    value: BN;
    constructor(value: any);
    static fromJson(data: {
        serializer: string;
        value: string;
    }): RadixEUID;
    equals(euid: RadixEUID): any;
    toJson(): {
        serializer: string;
        value: any;
    };
    toByteArray(): Buffer;
    toString(): string;
}
