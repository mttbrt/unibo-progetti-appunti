import { RadixEUID, RadixSignature, RadixBasicContainer, RadixECKeyPair } from '../RadixAtomModel';
export default abstract class RadixParticle extends RadixBasicContainer {
    static SERIALIZER: number;
    action: string;
    destinations: Array<RadixEUID>;
    owners: Array<RadixECKeyPair>;
    serializer: number;
    signatures: {
        [id: string]: RadixSignature;
    };
    constructor(json?: object);
}
