import { RadixKeyPair } from '../RadixAtomModel';
export default class RadixKeyStore {
    /**
     * Encrypt a private key
     * @param keyPair
     * @param password
     * @returns
     */
    static encryptKey(keyPair: RadixKeyPair, password: string): Promise<unknown>;
    /**
     * Decrypts an encrypted private key
     * @param fileContents
     * @param password
     * @returns key
     */
    static decryptKey(fileContents: any, password: string): Promise<RadixKeyPair>;
    private static calculateMac;
}
